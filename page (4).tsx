"use client"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Login(){const[p,setP]=useState("");const router=useRouter();return <main className="section"><div className="container"><h1>Admin Login</h1><form className="form" onSubmit={async e=>{e.preventDefault();const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:p})});if(r.ok)router.push("/admin");else alert("Password ትክክል አይደለም");}}><label className="field"><span>Password</span><input type="password" value={p} onChange={e=>setP(e.target.value)}/></label><button className="btn">Login</button></form></div></main>}