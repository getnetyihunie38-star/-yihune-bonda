import {db} from "../../lib/db"; import Link from "next/link";
export const dynamic="force-dynamic";
export default async function Products(){let p:any[]=[];try{p=await db()("SELECT id,name,price::float8 price,sizes,color,description,image_url FROM products ORDER BY created_at DESC") as any[]}catch{}return <main className="section"><div className="container"><h1>ምርቶች</h1>{!p.length?<div className="notice">እስካሁን ምርት የለም። Admin ገጽ ላይ ይጨምሩ።</div>:<div className="grid">{p.map(x=><article className="card" key={x.id}>{x.image_url?<img src={x.image_url} alt={x.name}/>:null}<div className="body"><h2>{x.name}</h2><div className="price">{Number(x.price).toLocaleString()} ብር</div><p className="muted">{x.color} • {(x.sizes||[]).join(", ")}</p><p>{x.description}</p><Link className="btn" href={"/order?product="+encodeURIComponent(x.name)}>ይዘዙ</Link></div></article>)}</div>}</div></main>}